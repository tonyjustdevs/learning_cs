using TPNS.SharedLibrary;
using System.IO.Compression;
partial class Program
{
    private static void Compress(string algorithm = "gzip")
    {
        //GZipStream gzip_smol_xml_obj = new(xml_file_stream,CompressionLevel.SmallestSize);
        //GZipStream
    }
    //BrotliStream
}

