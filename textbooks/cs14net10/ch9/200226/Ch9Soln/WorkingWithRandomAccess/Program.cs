
using Microsoft.Win32.SafeHandles;
using System.Data;
using System.Text;

using SafeFileHandle handle = File.OpenHandle("coffee.txt", FileMode.Create, FileAccess.Write);

//Byte[] some_buffer_bytes    = UTF8Encoding.UTF8.GetBytes("caphe den 40k");
//MemoryStream memory_stream  = new MemoryStream(some_buffer_bytes);

//var some_handle = File.OpenHandle(some_file_path, FileMode.Create, FileAccess.Write);


//var handle = File.OpenHandle(,);

// 1. open handle to file.txt

// 2. create string msg

// 3. create memory stream


